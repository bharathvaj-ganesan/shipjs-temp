{
  "name": "@ifaafa/shipjs-temp",
  "version": "1.0.1",
  "description": "sample package for testing shipjs",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "build": "npm run clean && mkdir dist && cp src/** dist",
    "shipjs:prepare": "shipjs prepare",
    "shipjs:trigger": "npm run build && shipjs trigger"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "shipjs": "0.23.0"
  }
}
